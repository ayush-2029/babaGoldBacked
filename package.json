{
  "name": "babagoldbackend",
  "version": "1.0.0",
  "description": "BabaGold API — public storefront reads and admin writes over S3-stored JSON",
  "main": "handler.js",
  "scripts": {
    "dev": "node local.js",
    "test": "node --test \"test/**/*.test.js\"",
    "deploy": "serverless deploy",
    "logs": "serverless logs -f api -t"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.1133.0",
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/client-ssm": "^3.1133.0",
    "@aws-sdk/s3-request-presigner": "^3.1133.0",
    "aws-jwt-verify": "^5.2.1",
    "express": "^5.2.1",
    "serverless-http": "^4.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
